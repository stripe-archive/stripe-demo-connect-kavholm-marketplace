import webpack from 'webpack';
import { WebpackEntrypoints } from './entries';
export default function getBaseWebpackConfig(dir: string, { dev, isServer, buildId, config, target, entrypoints, tracer, }: {
    tracer?: any;
    dev?: boolean;
    isServer?: boolean;
    buildId: string;
    config: any;
    target?: string;
    entrypoints: WebpackEntrypoints;
}): Promise<webpack.Configuration>;
